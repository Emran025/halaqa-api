# تدقيق اكتمال عقد OpenAPI

## الحكم المختصر

العقد الحالي مكتمل من ناحية **تغطية المسارات الأساسية وبنية OpenAPI**، لكنه ليس مكتملًا بعد من ناحية **التفاصيل النهائية لكل خاصية في كل Request وResponse**. تم التحقق آليًا من صحة OpenAPI 3.1.1 ووجود 50 مسارًا و68 عملية، لكن صحة المواصفة لا تعني أن جميع خصائص المجال قد حُسمت.

## ما هو مكتمل حاليًا

| المجال | مستوى التغطية |
|---|---|
| المصادقة والحسابات | الحقول الأساسية للتسجيل والدخول وكلمات المرور والملف الشخصي موجودة. |
| الحلقات | الاسم والوصف والحالة والمعلم وعدد الطلاب ومفاتيح الحلقة الأساسية موجودة. |
| التسجيل والقبول | الطلب والقرار والملاحظات وحالة الطلب موجودة. |
| الجلسات | الطرفان، الحلقة، النوع، الحالة، أوقات الاتصال، القبول والرفض والإنهاء ممثلة. |
| المصحف | السور، الصفحات، الآيات، الكلمات، رقم الصفحة، النطاق وموضع المصحف ممثلة. |
| الأخطاء | الآية، الصفحة، فهرس الكلمة، النوع، المصدر، الملاحظة، الجلسة والمهمة ممثلة. |
| الملاحظات والتقييم | الملاحظة والكاتب والتقييم والدرجة والتعليق ودور المقيم ممثلة. |
| التقارير والاعتماد | حالة التقرير، المهام، اعتماد المعلم، تأكيد الطالب وملاحظته ممثلة. |
| التقدم والسجل | نقاط التقدم، التقارير التاريخية، والأخطاء التاريخية ممثلة بواجهات قراءة. |
| WebSocket/WebRTC | يوجد عقد مساند للرسائل، لكنه ليس جزءًا أصليًا من OpenAPI ويحتاج اختبارًا تشغيليًا مستقلًا. |

## الفجوات التي تمنع اعتباره عقدًا نهائيًا

### خصائص الطلبات

بعض الطلبات تعبر عن الحد الأدنى الوظيفي فقط، ولا تزال خصائصها التفصيلية مرتبطة بقرارات المجال التي لم تُحسم في وثيقة المتطلبات. من ذلك تفاصيل جدول الحلقة، الحد الأقصى للطلاب، جدولة الجلسة، تكرار الجلسة، إعدادات السماح بالتنقل في المصحف، ومستوى الطالب عند إسناده إلى الحلقة.

كما أن `AssignStudentInput` يحتوي حاليًا على `student_id` فقط، بينما قد يحتاج العقد النهائي إلى تاريخ الانضمام، مستوى الطالب، ملاحظات الإسناد، وحالة العضوية. ويحتوي `UpdateReportInput` على `summary` فقط، بينما قد يتطلب التقرير ملخصًا تفصيليًا أو إجراءات اعتماد وإعادة فتح أكثر تحديدًا.

### خصائص الاستجابات

بعض الاستجابات تحتاج إلى توسيع قبل اعتمادها إنتاجيًا. فاستجابة الجلسة لا تعرض كل معلومات الاتصال والتفاوض داخل مورد الجلسة، بل تفصل إعدادات WebRTC في مورد آخر. وهذا قرار صحيح معماريًا، لكنه يجب أن يُثبت في العقد النهائي مع تحديد مدة صلاحية إعدادات ICE وآلية تجديدها.

كذلك يحتاج التقرير النهائي عادةً إلى خصائص صريحة مثل مدة الجلسة، عدد المهام، عدد الأخطاء حسب النوع، آخر مزامنة، نسخة التقرير، وقت الاعتماد، سبب إعادة الفتح، وحالة قفل التعديل. هذه الخصائص ليست جميعها مطلوبة صراحة في المتطلبات الحالية، ولذلك لم تُفترض تلقائيًا.

### الخصائص المشتركة والبروتوكول

العقد لا يفرض حتى الآن بصورة موحدة حقول `request_id`, `trace_id`, `created_by`, `updated_by`, أو رأس `Idempotency-Key`. يوجد `client_operation_id` في بعض أجسام الطلبات، لكنه ليس بديلًا موحدًا لكل عمليات الإنشاء والتحديث وإعادة المحاولة.

كما أن عقد pagination الحالي يحدد `meta` الأساسي، لكنه لا يفرض روابط `first`, `last`, `prev`, و`next` إذا كان العميل يحتاجها. ويجب اتخاذ قرار موحد بشأن شكل أخطاء Laravel في جميع حالات `401`, `403`, `404`, `409`, `422`, و`429` و`500`.

### الصلاحيات والحقول المشروطة بالدور

يصف العقد الأدوار والصلاحيات على مستوى العمليات، لكنه لا يصف بالتفصيل الحقول التي تظهر للمعلم ولا تظهر للطالب، أو الحقول التي يمكن تعديلها لكل دور. يجب أن تُثبت هذه الفروق إما في مخططات منفصلة مثل `TeacherUser` و`StudentUser`، أو في وصف واضح لحقول Resource المشروطة بالدور.

### الاتصال اللحظي

OpenAPI يصف REST، ولا يصف WebSocket بصورة معيارية كاملة. لذلك يوجد ملف `REALTIME_CONTRACT.md` كعقد مساند. إذا أريد عقد قابل للتوليد الآلي للعميل، فينبغي إضافة AsyncAPI مستقل لرسائل WebSocket، يتضمن القنوات، الرسائل، payload schemas، واتجاه الرسائل.

## قرارات يجب حسمها قبل الإصدار النهائي

| القرار | أثره على العقد |
|---|---|
| هل يملك المعلم حلقة واحدة أم عدة حلقات؟ | يؤثر في `GET /halaqas` وعلاقات المعلم. |
| هل يسمح للطالب بالانضمام إلى أكثر من حلقة؟ | يؤثر في العضويات والصلاحيات. |
| هل الجلسة فردية أم جماعية؟ | يؤثر في نموذج Session وWebRTC وعدد المشاركين. |
| هل اعتماد الطالب إلزامي أم تأكيد اطلاع؟ | يؤثر في حالات التقرير. |
| هل أخطاء الطالب رسمية أم مسودة حتى يعتمدها المعلم؟ | يؤثر في صلاحيات التعديل والحفظ. |
| هل يستطيع الطالب تعديل موضع المصحف بحرية؟ | يؤثر في `mushaf-state` وأحداث المزامنة. |
| ما حقول تقييم المعلم والطالب؟ | يؤثر في `Evaluation` ودرجة القياس. |
| هل الملاحظة مرتبطة بموضع قرآني؟ | يؤثر في `Note` وإضافة `ayah_id` و`word_index`. |
| ما مدة إعادة فتح التقرير؟ | تؤثر في Policy وحالة التقرير. |
| ما استراتيجية المصادقة؟ | تؤثر في bearer token، refresh token، وانتهاء الجلسة. |

## الخلاصة

لا ينبغي وصف العقد الحالي بأنه ناقص من حيث الهيكل؛ فهو يغطي وظائف النظام الرئيسية. لكن لا ينبغي أيضًا وصفه بأنه **مكتمل نهائيًا لكل خصائص الإرسال والاستقبال** قبل حسم القرارات السابقة وتحويلها إلى حقول إلزامية أو اختيارية، ثم إضافة نماذج الاستجابات التفصيلية وAsyncAPI للاتصال اللحظي.

الخطوة الصحيحة التالية هي اعتماد قرارات المجال، ثم إصدار `openapi.yaml` نهائي برقم إصدار جديد، وتحديث Laravel Form Requests وAPI Resources والاختبارات بناءً عليه.
