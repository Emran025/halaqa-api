# سياسة معمارية المشروع
## Quran Halaqa Live — المرجع الملزم

هذه الوثيقة هي المرجع الموحد لنطاق النظام وقراراته المعمارية. عند وجود تعارض بين اقتراح وكيل الذكاء الاصطناعي وأي قاعدة هنا، تُقدَّم هذه السياسة والعقد المحدث على الاقتراح.

## نطاق النظام

النظام منصة مباشرة لحلقات تحفيظ القرآن عن بعد. الأدوار الوحيدة هي `teacher` و`student`. تشمل الوظائف الحلقات، تسجيل الطلاب، كود المعلم، قبول الطالب، ملفات التعلم، خطط الحفظ والمراجعة والتلاوة، الجدولة، التنبيهات، الجلسات المباشرة، المصحف التفاعلي، الأخطاء، التقييم، الملاحظات، التقارير والاعتماد.

لا يوجد في النطاق دور مشرف أو مدير نظام أو موظف إداري أو مدرسة أو ولي أمر أو دفع أو شهادات أو مراجعة آلية بالذكاء الاصطناعي.

## القرار الخلفي

Laravel هو الـbackend الأولي والوحيد، وهو مسؤول عن REST API والمصادقة والسياسات والحفظ الرسمي والاستعلامات والتقارير وتشغيل WebSocket الداخلي والإشارة إلى WebRTC. لا يضاف backend آخر للمشروع.

WebSocket جزء من تطبيق Laravel نفسه، وينفذ داخل المستودع في `app/Realtime` مع أمر Artisan للتشغيل. لا يستخدم المشروع Laravel Broadcasting عبر مزود خارجي، ولا Reverb أو Pusher أو Soketi أو Socket.IO أو SIPSorcery أو أي مكتبة أو خدمة خارجية للاتصال اللحظي.

## القرار اللحظي

الصوت والصورة وبيانات التفاعل المباشر تنتقل P2P مباشرة بين جهاز المعلم وجهاز الطالب. Laravel لا يستقبل الصوت أو الفيديو ولا يعيد إرساله ولا يعمل Media Server أو Relay أو Proxy. WebSocket Laravel يمرر الإشارات وحالة القناة فقط، ولا ينقل الوسائط.

لا يستخدم المشروع STUN أو TURN أو ICE Relay. تقتصر المرشحات على Host ICE Candidates. إذا لم يتوافر اتصال مباشر، تفشل الجلسة بالحالة `direct_connection_unavailable` ولا يُسمح ببديل مرحل أو طرف ثالث.

## البيانات الرسمية

تحفظ في Laravel الجلسة، العضوية، نطاق المهمة، حالة المصحف الرسمية للجلسة، الأخطاء، التقييمات، الملاحظات، التقارير، الاعتمادات، خطة المتابعة، أوقات الحضور، عناصر الجدولة والتنبيهات. تنتقل الصفحة والمؤشر وطلب إعادة القراءة لحظيًا، لكن لا يصبح التغيير رسميًا إلا بعد حفظه عبر REST وService في `session_mushaf_states` أو المورد الرسمي المقابل.

لا تحفظ في السجل التعليمي ملفات الصوت أو الفيديو أو SDP أو ICE. يستخدم كل تغيير قابل لإعادة المحاولة `client_operation_id` أو `Idempotency-Key`.

## التسجيل والخصوصية

يجمع تسجيل الطالب البيانات الشخصية وبيانات التواصل وبيانات الحفظ السابق وخطة الحفظ والمراجعة والتلاوة وأوقات الحضور والتكرار. يمكن للطالب إدخال كود معلم، أو تركه فارغًا.

عند وجود الكود، يوجه الطلب إلى المعلم المحدد. عند فراغه، يظهر الطلب للمعلمين المؤهلين كافة. قبل القبول لا يظهر إلا `ApplicantPublicSummary` دون البريد والهاتف وWhatsApp وبيانات الحفظ والخطة وأوقات الحضور. بعد القبول وإنشاء العلاقة التعليمية، يظهر الملف التفصيلي للطالب نفسه ولمعلمه المرتبط فقط، ولا يظهر لبقية المعلمين.

لا توجد موافقة إدارية على تسجيل المعلم ضمن النطاق الحالي. يحتوي ملف المعلم على البيانات الشخصية والتواصل والمؤهل والخبرة والسيرة والقدرة الاستيعابية والوثائق.

## قاعدة Laravel الطبقية

```text
Route
  -> Form Request
  -> Policy / Channel Authorization
  -> Thin Controller أو WebSocket Handler
  -> Service أو Query Service
  -> Model / Transaction / Event / Notification / Job
  -> API Resource
  -> JSON Response أو رسالة قناة مصرح بها
```

توضع API Resources في `app/Http/Resources`، وForm Requests في `app/Http/Requests`، وControllers في `app/Http/Controllers`، وServices في `app/Services`، وQueries في `app/Queries`، وPolicies في `app/Policies`، ومكونات الاتصال الداخلي في `app/Realtime`.

## مرجعية العقود

| الترتيب | الملف | الاستخدام |
|---:|---|---|
| 1 | `PROJECT_ARCHITECTURE_POLICY.md` | القرارات غير القابلة للتفاوض ونطاق المشروع. |
| 2 | `openapi.yaml` | عقد REST API والطلبات والاستجابات. |
| 3 | `DATABASE_SCHEMA_CONTRACT.md` و`database_schema.sql` | عقد MySQL وقاموس الحقول والعلاقات والقيود. |
| 4 | `DATABASE_API_ALIGNMENT.md` | مواءمة الجداول مع مخططات OpenAPI وعمليات Laravel. |
| 5 | `REALTIME_CONTRACT.md` | عقد WebSocket وWebRTC P2P. |
| 6 | `API_FUNCTIONS_CATALOG.md` | فهرس الوظائف والمسارات والصلاحيات. |
| 7 | `references/canonical-tree.md` | الشجرة المعيارية للمجلدات. |
| 8 | `references/placement-rules.md` | قواعد توزيع المسؤوليات. |
| 9 | `references/validation-checklist.md` | شروط المراجعة والرفض. |

## عقد API الصريح

يجب أن يعرّف `openapi.yaml` كل حقل في الطلب والاستجابة باسم واضح ونوع وقيمة مطلوبة وحدود وقيم تعداد عند الحاجة. لا تستخدم الاستجابات خاصية عامة باسم `data`؛ يستخدم كل مورد اسمه الدلالي، وتستخدم القوائم أسماء جمع واضحة مثل `students` و`sessions` و`mistakes`. تُغلق أجسام المجال بـ`additionalProperties: false`، ولا تستخدم خرائط ديناميكية إلا بمخطط صريح ومبرر.

## قاعدة التغيير

لا يقبل الوكيل ميزة أو Endpoint أو حدثًا جديدًا قبل تحديد دوره ومدخلاته ومخرجاته وصلاحيته ومكانه في الشجرة. أي تغيير في عقد REST أو WebSocket يجب أن يحدّث الملفات ذات الصلة ويشغل أدوات التحقق ويضيف اختبارًا لحالات النجاح والفشل وعدم الصلاحية والتكرار وانقطاع الاتصال المباشر.


**وسم القرار الملزم:** `Laravel-only` للتحكم والإشارة، و`P2P-only` للصوت والصورة وبيانات التفاعل المباشر.
